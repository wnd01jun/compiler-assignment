int main(void){
    int i = 0;
    do {
        int k = 0;
        int w = k;
    } while(i <= 10);

    printf("%d", i);

    return 0;
}// 에러메시지가 잘 출력되도록(변수 중복, 변수 없이 사용하지 않고) -> test , lee.l -> 고치기,
// tree가 잘 만들어지는 것도 중요하고, 에러가 있는 프로그램도 잘 되는지 테스트했습니다.  