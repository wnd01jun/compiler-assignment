단항 연산자 전 피연산자에 대해 C언어의 기본 변환 규칙을 적용 (정수 승격, 배열/함수 → 포인터)| 개념                                | 설명                                                       |
| --------------------------------- | -------------------------------------------------------- |
| **lvalue**                        | 메모리 위치를 가지는 값. 좌변 가능. 변수, 배열 원소, 포인터 역참조 등이 이에 해당        |
| **convertUsualUnaryConversion()** | 단항 연산자 전 피연산자에 대해 C언어의 기본 변환 규칙을 적용 (정수 승격, 배열/함수 → 포인터) |
convertUsualBinaryConversion()	이항 연산	두 피연산자 타입 맞추기	더 넓은 타입으로 변환
convertUsualAssignmentConversion()	대입 연산	우변을 좌변 타입에 맞게 변환